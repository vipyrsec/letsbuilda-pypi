name: "Python - Publish to PyPI"

on:
  release:
    types: [released]

jobs:
  publish:
    permissions:
      id-token: write
    uses: darbiadev/.github/.github/workflows/python-pypi-publish.yaml@8209a320c942ec889f2f73752bebdcb2806792e5
